# CODEX_SPEC — LPの記述を計算エンジンの実装に合わせる

## 目的

`index.html` に、アプリ本体（realsim-app）の計算エンジンと食い違う記述と、サンプル物件を計算しても再現できない数値が載っている。これを実装どおりの記述・数値に直す。

数値はすべて、realsim-app の `src/demoData.js` の `DEMO_PROPERTY_PARAMS`（アプリ内の名前は「サンプル: 文京区 1R」）を、`src/engine.js`（PR#19 マージ後の main）で計算した値である。

## 範囲

**`index.html` の、下に列挙した29箇所の文字列置換だけを行う。**

- 各項目の「変更前」の文字列を、「変更後」の文字列にそのまま置き換える。どの変更前文字列も `index.html` 内にちょうど1回だけ出現する
- 置換以外の変更をしない。レイアウト、CSS、クラス名の追加、SVGのパス形状、JavaScript（30秒診断の計算式を含む）、`<head>` 内のメタ情報には触れない
- 他のファイルを作成・変更しない

スクリーンショットは不要です。ブラウザでの画面確認も不要です。

## 置換一覧

### 1. ダッシュボード見出し

変更前:

```html
港区 1R ¥24,800,000
```

変更後:

```html
文京区 1R ¥24,800,000
```

### 2. ダッシュボード 月CF

変更前:

```html
<b class="dn">−12,480<em>円</em></b>
```

変更後:

```html
<b>+6,081<em>円</em></b>
```

### 3. ダッシュボード DSCR

変更前:

```html
<span>DSCR</span><b>1.02</b>
```

変更後:

```html
<span>DSCR</span><b>1.09</b>
```

### 4. ダッシュボード IRR

変更前:

```html
<span>IRR 35Y</span><b>4.2%</b>
```

変更後:

```html
<span>IRR 35Y</span><b>5.2%</b>
```

### 5. ダッシュボード 実質利回り（アプリに無い指標）

変更前:

```html
<span>実質利回り</span><b>3.68%</b>
```

変更後:

```html
<span>表面利回り</span><b>4.45%</b>
```

### 6. ダッシュボード 最適売却年

変更前:

```html
<span>最適売却年</span><b>13<em>年目</em></b>
```

変更後:

```html
<span>最適売却年</span><b>35<em>年目</em></b>
```

### 7. ダッシュボード シナリオ差

変更前:

```html
<b class="dn">412<em>万円</em></b>
```

変更後:

```html
<b class="dn">795<em>万円</em></b>
```

### 8. 吹き出し

変更前:

```html
業者の試算より <b class="big">−412万円</b> ズレています。<br>原因は「金利上昇」と「退去2回ぶんの原状回復費」でした。
```

変更後:

```html
業者の試算より <b class="big">−795万円</b> ズレています。<br>原因の大半は「家賃下落（約400万円）」と「退去・空室（約200万円）」でした。
```

### 9. YIELD チップ

変更前:

```html
<span class="chip">4.45%</span>
```

変更後:

```html
<span class="chip">5.2%</span>
```

### 10. YIELD 箇条書き

変更前:

```html
<li>表面 / NOI / 実質利回りを併記</li>
```

変更後:

```html
<li>表面利回り・単年CF・累積CFを表示</li>
```

### 11. OCCUPANCY チップ

変更前:

```html
<span class="chip">96%</span>
```

変更後:

```html
<span class="chip">97%</span>
```

### 12. MORTGAGE チップ

変更前:

```html
<span class="chip">1.02</span>
```

変更後:

```html
<span class="chip">1.09</span>
```

### 13. MORTGAGE 本文

変更前:

```html
元利均等420回の返済表を引き直します。デッドクロスも可視化。
```

変更後:

```html
毎年、その年の金利で返済額を引き直します。
```

### 14. MORTGAGE 箇条書き

変更前:

```html
<li>DSCR（返済余裕率）を毎年判定</li>
```

変更後:

```html
<li>初年度DSCR（返済余裕率）で判定</li>
```

### 15. UPKEEP チップ

変更前:

```html
<span class="chip">13年</span>
```

変更後:

```html
<span class="chip">12年</span>
```

### 16. 4変数セクションの注記

変更前:

```html
※ 数値は港区1R・2,480万円・想定家賃92,000円のサンプル物件をYOMUの初期前提で計算した例です。
```

変更後:

```html
※ 数値は、アプリに入っているサンプル物件（文京区1R・2,480万円・想定家賃92,000円）を計算した結果です（2026年9月版の計算仕様）。
```

### 17. 30秒診断 ラベル

変更前:

```html
<span>35年累計CF</span>
```

変更後:

```html
<span>月CF×420ヶ月</span>
```

### 18. 比較表 原状回復費

変更前:

```html
退去のたびに㎡単価×面積で自動計上
```

変更後:

```html
退去のたびに設定した金額を計上
```

### 19. 比較表 設備交換

変更前:

```html
設備ごとの周期×費用を台帳管理し、その月に計上
```

変更後:

```html
設備ごとの交換周期と費用を設定し、交換の年に計上
```

### 20. 比較表 税

変更前:

```html
限界税率で税引後CF。減価償却切れの反転年まで表示
```

変更後:

```html
限界税率で税引後CF。償却が切れた年からの税負担増も反映
```

### 21. 比較表 売却出口

変更前:

```html
1〜35年の全売却年を総当たりし、最適年とIRRを提示
```

変更後:

```html
3〜35年目の売却年を総当たりし、最適年を提示。IRRは売却込みで算出
```

### 22. 機能01 グラフ内ラベル

変更前:

```html
text-anchor="end">412万円</text>
```

変更後:

```html
text-anchor="end">795万円</text>
```

### 23. 機能01 タグ

変更前:

```html
<span>IRR 4.2%</span><span class="r">差額 412万円</span>
```

変更後:

```html
<span>IRR 5.2%</span><span class="r">差額 795万円</span>
```

### 24. 機能03 信号機の例

変更前:

```html
DSCR 1.02 — <b class="neg">金利+1%で赤字転落</b>
```

変更後:

```html
DSCR 1.09 — <b class="neg">金利+1%で初年度から持ち出し</b>
```

### 25. 機能05 本文

変更前:

```html
売却年を1〜35年まで総当たりして最適な出口を計算します。
```

変更後:

```html
売却年を3〜35年目まで総当たりして最適な出口を計算します。
```

### 26. 機能05 比較表の行

変更前:

```html
<td style="padding:5px 0">港区1R</td><td>4.2%</td><td>1.02</td><td>-1.4%</td>
```

変更後:

```html
<td style="padding:5px 0">文京区1R</td><td>5.2%</td><td>1.09</td><td>2.7%</td>
```

### 27. 計算エンジン SIMULATION SPAN

変更前:

```html
35年を年次ではなく月次で計算。金利改定も退去も、起きた月に反映されます。
```

変更後:

```html
35年を月単位で計算。退去・空室・返済は起きた月に、金利と家賃の水準は毎年反映します。
```

### 28. 計算エンジン EXIT SEARCH

変更前:

```html
<b>35<i>通り</i></b><p>売却年を1年目から35年目まで総当たりし、IRR最大の出口を特定。</p>
```

変更後:

```html
<b>33<i>通り</i></b><p>売却年を3年目から35年目まで総当たりし、総合損益が最大になる出口を特定。</p>
```

### 29. 計算エンジン STRESS TEST

変更前:

```html
金利・空室・家賃・修繕・税制・売却価格を同時に振って耐性を測定。
```

変更後:

```html
金利・空室・家賃・修繕・原状回復・売却価格を同時に振って耐性を測定。
```

## 完了条件（Codexが判定する）

以下はすべて `index.html` に対して実行する。件数は `grep -o … | wc -l`（出現回数）で数える。

| コマンド | 期待値 |
|---|---|
| `grep -o "港区" index.html \| wc -l` | 0 |
| `grep -o "412" index.html \| wc -l` | 0 |
| `grep -o "4\.2%" index.html \| wc -l` | 0 |
| `grep -o "1\.02" index.html \| wc -l` | 0 |
| `grep -o "反転年\|IRR最大\|税制\|㎡単価\|デッドクロスも可視化\|毎年判定\|起きた月に反映されます\|35年累計CF\|その月に計上\|3\.68%" index.html \| wc -l` | 0 |
| `grep -o "文京区" index.html \| wc -l` | 3 |
| `grep -oF "795万円" index.html \| wc -l` | 3 |
| `grep -oF "795<em>万円</em>" index.html \| wc -l` | 1 |
| `grep -o "1\.09" index.html \| wc -l` | 4 |
| `grep -o "5\.2%" index.html \| wc -l` | 4 |
| `grep -oF "+6,081" index.html \| wc -l` | 1 |
| `grep -oF "33<i>通り</i>" index.html \| wc -l` | 1 |
| `git diff --stat` | 変更ファイルは `index.html` のみ。29行の変更（29 insertions, 29 deletions） |

ビルド工程があるリポジトリなら、そのビルドが成功すること。

## 人間がプレビューで確認すること（Codexは判定しない）

- ヒーローのダッシュボードで、月CFが赤字色でなく通常色の「+6,081円」になっている
- 吹き出しの2行目がスマホ幅で不自然に折り返していない
- 「計算エンジン」節の EXIT SEARCH が「33通り」になっている
